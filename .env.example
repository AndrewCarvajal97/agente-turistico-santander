# ==========================================================================
# Configuración del Agente IA - Guía Turística de Santander
# Copia este archivo a ".env" y completa tus valores reales.
# NUNCA subas el archivo .env al repositorio.
# ==========================================================================

# --- Proveedor de IA: Google Gemini ---
# Obtén tu clave gratuita en: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=tu_api_key_aqui

# --- Modelo de chat ---
GEMINI_CHAT_MODEL=gemini-flash-latest

# --- Documento fuente ---
PDF_PATH=data/guia_turistica_santander.pdf

# --- Memoria de conversaciones por sesión (archivos persistentes) ---
MEMORY_DIR=data/memoria
# Tamaño (caracteres) del bloque reciente antes de resumir la memoria.
MEMORY_MAX_CHARS=2500
# Mínimo de intercambios recientes que se conservan siempre de forma literal.
MEMORY_MIN_TURNS=3
